// Deprecated: this module has moved to github.com/maloquacious/phrases.
// The replacement draws passphrases from crypto/rand rather than
// math/rand/v2, and its Generate returns (string, error).
module github.com/mdhender/phrases

go 1.22
